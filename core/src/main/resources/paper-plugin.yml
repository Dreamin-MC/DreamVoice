name: DreamVoice
version: $version
main: fr.dreamin.dreamvoice.core.DreamVoice
api-version: 26.1.2
authors: [ "Dreamin" ]
dependencies:
  server:
    LuckPerms:
      required: true
      load: BEFORE
    voicechat:
      required: true
      load: BEFORE